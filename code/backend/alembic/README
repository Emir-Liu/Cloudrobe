# Alembic迁移目录

此目录包含数据库迁移脚本。

## 使用方法

### 初始化数据库

```bash
# 生成初始迁移
alembic revision --autogenerate -m "init"

# 执行迁移
alembic upgrade head
```

### 创建新的迁移

```bash
# 创建新迁移(自动检测模型变化)
alembic revision --autogenerate -m "描述变更"

# 手动创建迁移脚本
alembic revision -m "描述变更"
```

### 执行迁移

```bash
# 升级到最新版本
alembic upgrade head

# 升级到指定版本
alembic upgrade <revision_id>

# 回滚一个版本
alembic downgrade -1

# 回滚到指定版本
alembic downgrade <revision_id>

# 查看迁移历史
alembic history

# 查看当前版本
alembic current
```

## 注意事项

1. 修改模型后,记得运行`alembic revision --autogenerate -m "描述"`
2. 执行迁移前,先review生成的迁移脚本
3. 不要手动修改已执行的迁移脚本
4. 生产环境执行迁移前,务必备份数据库
